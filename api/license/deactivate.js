/* See activate.js — one handler serves activate, validate, and deactivate,
   branching on the path segment. Re-exported so each has its own route. */
export { default, config } from "./activate.js";
